// Var definitionsvar a=0;var link = window.location;var google = "www.google.com";// Functionsfunction count() { // Count is temporary function    a++;    document.getElementById('demo').textContent = a;}function getpassword() { // Testing putting password back	if (true) {	document.getElementById("password").textContent = "SAh2r)$#x";	}}chrome.tabs.getSelected(null, function(tab) { // Get domain of current tab		var domain = new URL(tab.url)	    document.getElementById("domain").textContent = (domain.hostname);	})// Call to functions onclickdocument.getElementById('do-count').onclick = count; // Countdocument.getElementById('getpassword').onclick = getpassword; // Get password